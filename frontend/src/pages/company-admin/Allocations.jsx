const Allocations = () => (
  <div>
    <h1 className="h3 fw-bold mb-3">Allocations</h1>
    <section className="bg-white border rounded p-4 text-secondary">
      Asset allocation records will appear here when allocation APIs are added.
    </section>
  </div>
);

export default Allocations;
