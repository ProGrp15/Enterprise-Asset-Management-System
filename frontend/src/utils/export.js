export function exportCsv(filename, rows){const list=Array.isArray(rows)?rows:[];const keys=[...new Set(list.flatMap(r=>Object.keys(r||{})))];const esc=v=>`"${String(v??'').replaceAll('"','""')}"`;const csv=[keys.map(esc).join(','),...list.map(r=>keys.map(k=>esc(r[k])).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download=filename.endsWith('.csv')?filename:`${filename}.csv`;a.click();URL.revokeObjectURL(a.href)}
export function printRows(title,rows){const keys=[...new Set((rows||[]).flatMap(r=>Object.keys(r||{})))];const html=`<html><head><title>${title}</title><style>body{font:14px Arial;padding:24px}table{border-collapse:collapse;width:100%}th,td{border:1px solid #ddd;padding:7px;text-align:left}th{background:#f1f5f9}</style></head><body><h1>${title}</h1><table><thead><tr>${keys.map(k=>`<th>${k}</th>`).join('')}</tr></thead><tbody>${(rows||[]).map(r=>`<tr>${keys.map(k=>`<td>${r[k]??''}</td>`).join('')}</tr>`).join('')}</tbody></table><script>window.print()</script></body></html>`;const w=window.open('','_blank');if(w){w.document.write(html);w.document.close()}}
export function parseCsv(text){const lines=text.trim().split(/\r?\n/).filter(Boolean);if(!lines.length)return [];const split=s=>s.split(/,(?=(?:[^"]*"[^"]*")*[^"]*$)/).map(v=>v.replace(/^"|"$/g,'').replaceAll('""','"'));const headers=split(lines[0]);return lines.slice(1).map(line=>{const cells=split(line);return Object.fromEntries(headers.map((h,i)=>[h,cells[i]??'']))})}
