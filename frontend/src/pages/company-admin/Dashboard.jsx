import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { FaArrowRight, FaBoxOpen, FaClipboardList, FaPlus, FaUserShield, FaUsers } from 'react-icons/fa';
import { getCompanyAdminDashboard } from '../../services/dashboardService';

const cards = [['Employees','employees',FaUsers,'People in this workspace'],['Company admins','admins',FaUserShield,'Workspace administrators'],['Assets','assets',FaBoxOpen,'Tracked in inventory'],['Open requests','requests',FaClipboardList,'Awaiting review']];
const statusCounts = rows => rows.reduce((out,row)=>{const key=String(row.status||'UNKNOWN').toUpperCase();out[key]=(out[key]||0)+1;return out;},{});
export default function Dashboard(){
  const [data,setData]=useState(null); const [error,setError]=useState('');
  useEffect(()=>{getCompanyAdminDashboard().then(setData).catch(e=>setError(e.response?.data?.message||'Unable to load company dashboard.'));},[]);
  const stats=data?.stats||{}; const statuses=statusCounts(data?.assets||[]); const max=Math.max(1,...Object.values(statuses));
  return <><div className="page-heading d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><div className="eyebrow">Company overview</div><h1>{data?.company?.company_name||'Company dashboard'}</h1><p>Live operational visibility across people, assets, and requests.</p></div><div className="d-flex gap-2"><Link className="btn btn-light border" to="/company-admin/assets"><FaBoxOpen className="me-2"/>Asset inventory</Link><Link className="btn btn-primary" to="/company-admin/employees"><FaPlus className="me-2"/>Add employee</Link></div></div>{error&&<div className="alert alert-danger">{error}</div>}<div className="row g-3 mb-4">{cards.map(([label,key,Icon,copy])=><div className="col-sm-6 col-xl-3" key={key}><div className="surface stat-card h-100"><div className="d-flex justify-content-between"><span className="muted">{label}</span><span className="stat-icon"><Icon/></span></div><div className="stat-value">{data?stats[key]??0:<span className="skeleton d-inline-block" style={{width:65,height:28}}/>}</div><small className="muted">{copy}</small></div></div>)}</div><div className="row g-4"><div className="col-xl-8"><section className="surface p-4 h-100"><div className="d-flex justify-content-between"><div><h5>Asset status</h5><p className="muted mb-0">Current inventory distribution from the asset service.</p></div><Link className="btn btn-sm btn-light border" to="/company-admin/assets">View inventory</Link></div><div className="d-grid gap-3 mt-4">{Object.entries(statuses).length?Object.entries(statuses).map(([status,count])=><div key={status}><div className="d-flex justify-content-between small mb-1"><span>{status.replaceAll('_',' ')}</span><strong>{count}</strong></div><div className="progress" style={{height:8}}><div className="progress-bar" style={{width:`${count/max*100}%`}}/></div></div>):<p className="muted mb-0">No assets have been registered yet.</p>}</div></section></div><div className="col-xl-4"><section className="surface p-4 h-100"><h5>Company summary</h5><div className="mt-4 d-grid gap-3"><div><small className="muted d-block">Industry</small><strong>{data?.company?.industry||'Not specified'}</strong></div><div><small className="muted d-block">Company size</small><strong>{data?.company?.company_size||'Not specified'}</strong></div><div><small className="muted d-block">Official email</small><strong>{data?.company?.official_email||'—'}</strong></div></div></section></div></div><section className="surface p-4 mt-4"><div className="d-flex justify-content-between align-items-center"><div><h5 className="mb-1">Quick actions</h5><p className="muted mb-0">Keep your workspace moving.</p></div></div><div className="row g-3 mt-1">{[['Invite employee','/company-admin/employees'],['Manage asset requests','/company-admin/asset-requests'],['Add a vendor','/company-admin/vendors']].map(([text,to])=><div className="col-md-4" key={to}><Link className="d-flex align-items-center justify-content-between border rounded-3 p-3" to={to}>{text}<FaArrowRight className="text-primary"/></Link></div>)}</div></section></>;
}
