import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { FaBoxOpen,FaClipboardList,FaUsers,FaUserShield,FaArrowRight,FaPlus } from 'react-icons/fa'; import { getCompanyAdminDashboard } from '../../services/dashboardService';
const cards=[['Employees','employees',FaUsers,'Manage people and access'],['Company admins','admins',FaUserShield,'Workspace administrators'],['Assets','assets',FaBoxOpen,'Tracked in inventory'],['Open requests','requests',FaClipboardList,'Awaiting review']];
export default function Dashboard(){const [data,setData]=useState(null),[error,setError]=useState('');useEffect(()=>{getCompanyAdminDashboard().then(setData).catch(e=>setError(e.response?.data?.message||'Unable to load company dashboard.'))},[]);const s=data?.stats||{};return <><div className="page-heading d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><div className="eyebrow">Company overview</div><h1>{data?.company?.company_name||'Company dashboard'}</h1><p>Stay on top of people, assets and operational work.</p></div><div className="d-flex gap-2"><Link className="btn btn-light border" to="/company-admin/assets"><FaBoxOpen className="me-2"/>Asset inventory</Link><Link className="btn btn-primary" to="/company-admin/employees"><FaPlus className="me-2"/>Add employee</Link></div></div>{error&&<div className="alert alert-danger">{error}</div>}<div className="row g-3 mb-4">{cards.map(([label,key,Icon,copy])=><div className="col-sm-6 col-xl-3" key={key}><div className="surface stat-card h-100"><div className="d-flex justify-content-between"><span className="muted">{label}</span><span className="stat-icon"><Icon/></span></div><div className="stat-value">{data?s[key]??0:<span className="skeleton d-inline-block" style={{width:65,height:28}}/>}</div><small className="muted">{copy}</small></div></div>)}</div><div className="row g-4"><div className="col-xl-8"><section className="surface p-4 h-100"><div className="d-flex justify-content-between"><div><h5>Asset activity</h5><p className="muted mb-0">Lifecycle activity will appear here as assets are added.</p></div><button className="btn btn-sm btn-light border">Last 30 days</button></div><div className="dashboard-chart mt-4" aria-label="Asset activity chart placeholder"/></section></div><div className="col-xl-4"><section className="surface p-4 h-100"><h5>Company summary</h5><div className="mt-4 d-grid gap-3"><div><small className="muted d-block">Industry</small><strong>{data?.company?.industry||'Not specified'}</strong></div><div><small className="muted d-block">Company size</small><strong>{data?.company?.company_size||'Not specified'}</strong></div><div><small className="muted d-block">Official email</small><strong>{data?.company?.official_email||'—'}</strong></div></div></section></div></div><section className="surface p-4 mt-4"><div className="d-flex justify-content-between align-items-center"><div><h5 className="mb-1">Quick actions</h5><p className="muted mb-0">Keep your workspace moving.</p></div></div><div className="row g-3 mt-1">{[['Invite employee','/company-admin/employees'],['Manage asset requests','/company-admin/asset-requests'],['Add a vendor','/company-admin/vendors']].map(([text,to])=><div className="col-md-4" key={to}><Link className="d-flex align-items-center justify-content-between border rounded-3 p-3" to={to}>{text}<FaArrowRight className="text-primary"/></Link></div>)}</div></section></>}
