# Backend modules inherit the repository-wide rules from the root .gitignore.
# Keep this file for tools that use the backend directory as their project root.

**/target/
**/build/
**/out/
**/*.class
**/*.jar
**/*.log
**/*.env
!**/.env.example
.metadata/
.classpath
.project
.settings/
.idea/
*.iml
