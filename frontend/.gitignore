# Frontend modules inherit the repository-wide rules from the root .gitignore.
# Keep this file for tools that use the frontend directory as their project root.

node_modules/
dist/
dist-ssr/
coverage/
.env
.env.*
!.env.example
*.local
*.log
.vite/
.cache/
